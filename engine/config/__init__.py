"""Declarative configuration: contracts, charges, margin, slippage, liquidity.

Every module here is either MEASURED from the serving database (and says so) or an
explicit ASSUMPTION with a stated basis (and says that). Nothing is a bare constant.
"""
